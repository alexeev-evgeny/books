# Books catalog
AngularJS, CoffeeScript, Slim, Scss

Install dependences:
```
gem install slim
npm install -g gulp
npm install
```

Compile build:
```
gulp compile
```

Start local webserver:
```
gulp start
```